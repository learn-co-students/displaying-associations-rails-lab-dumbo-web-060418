<h1>Artists</h1>

<ul>
<% @artists.each do |artist| %>
<h3><%= link_to artist.name, artist_path(artist) %></h3><br>
<p><%= artist.song_count %> <%= "song".pluralize(artist.song_count) %><br>
  <% end %>
</ul>
